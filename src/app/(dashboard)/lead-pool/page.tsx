import { LeadPoolOverview } from "@/components/features/lead-pool/lead-pool-overview"

export const metadata = { title: "คลังลีด" }

export default function LeadPoolPage() {
  return <LeadPoolOverview />
}
